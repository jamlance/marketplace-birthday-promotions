# marketplace-birthday-promotions

Deploy repo for the **birthday-promotions** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs birthday-promotions --push`.

Coolify: base_directory `/`, dockerfile `/apps/birthday-promotions/Dockerfile`.
